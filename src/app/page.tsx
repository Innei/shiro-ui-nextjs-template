import { StyledButton } from 'shiro-rc'

export default function Home() {
  return (
    <div className="mt-12 max-w-[80ch] flex items-center justify-center">
      <StyledButton>Click Me</StyledButton>
    </div>
  )
}
